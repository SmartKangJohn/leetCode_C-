#pragma once
#include <vector>
#include <set>
#include <queue>
#include <stack>
#include <string>

//hashset
#include <unordered_set>

#include <iostream>

using namespace std;

#define max(a,b) (((a) > (b)) ? (a) : (b))
#define min(a,b) (((a) < (b)) ? (a) : (b))